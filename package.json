{
  "name": "sports-green-moove",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "functions",
    "admin",
    "website"
  ],
  "scripts": {
    "build": "npm run build --workspace functions && npm run build --workspace admin && npm run build --workspace website",
    "build:website": "npm run build --workspace website",
    "test": "npm run test --workspace functions",
    "test:android-native": "cd android && gradle :app:testDebugUnitTest --no-daemon",
    "test:android-ui-build": "cd android && gradle :app:assembleDebugAndroidTest --no-daemon",
    "test:android-ui": "cd android && gradle :app:connectedDebugAndroidTest --no-daemon",
    "test:ios-native": "bash scripts/check-ios-native-plan.sh",
    "test:native-ui-coverage": "node tests/native-ui-coverage.test.mjs",
    "test:store-readiness": "node tests/store-readiness.test.mjs",
    "test:provider-readiness": "node tests/provider-readiness.test.mjs",
    "test:admin-plan-coverage": "node tests/admin-plan-coverage.test.mjs",
    "test:release-evidence-template": "node tests/release-evidence-template.test.mjs",
    "validate:provider-readiness": "node scripts/check-provider-readiness.mjs",
    "validate:release-evidence": "node scripts/validate-release-evidence.mjs",
    "test:rules": "firebase emulators:exec --project sports-green-moove-rules-test --only firestore,database,storage \"vitest run tests/security-rules.test.ts\"",
    "test:stripe-webhook-flow": "firebase emulators:exec --project sports-green-moove-rules-test --only firestore \"vitest run tests/stripe-webhook-flow.test.ts\"",
    "test:all": "npm test && npm run test:android-native && npm run test:android-ui-build && npm run test:ios-native && npm run test:native-ui-coverage && npm run test:store-readiness && npm run test:provider-readiness && npm run test:admin-plan-coverage && npm run test:release-evidence-template && npm run test:rules && npm run test:stripe-webhook-flow"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "firebase-tools": "^15.20.0"
  }
}
